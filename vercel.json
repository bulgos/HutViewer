{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "bun install",
  "buildCommand": "bun run build",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/hut-reservation-api/(.*)",
      "destination": "/api/hut-reservation?path=$1"
    }
  ]
}
